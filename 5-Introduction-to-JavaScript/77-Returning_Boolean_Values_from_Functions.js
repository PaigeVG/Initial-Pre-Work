// conditionals are pointless if you just want to return the boolean result of some expression. Just return the bool.

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);