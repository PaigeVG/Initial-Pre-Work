// JS strings are immutable. This does not, of course, mean that variables can't be reassigned to different string literals, but you can't re-assign any of the characters making them up.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World";

